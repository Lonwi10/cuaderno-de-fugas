/* Config opcional del repositorio.
   Si TODOS los del grupo van a usar la misma hoja y no te importa que la URL
   quede escrita en el repositorio, ponla aquí y nadie tendrá que pegarla:

     window.CDF_CONFIG = { url: 'https://script.google.com/macros/s/AKfy…/exec' };

   Si el repositorio es público, mejor déjalo vacío: cada uno pega la URL una
   vez en Ajustes y se queda guardada en su navegador. */
window.CDF_CONFIG = { url: '' };
